FROM node:22-alpine AS build
WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

RUN npx ng build components && \
    npx ng build api && \
    npx ng build features && \
    npx ng build origin-hair-collective-coming-soon --configuration azure

FROM nginx:alpine

COPY --from=build /app/dist/origin-hair-collective-coming-soon/browser /usr/share/nginx/html
COPY nginx/coming-soon.conf.template /etc/nginx/templates/default.conf.template

EXPOSE 80

ENV PORT=80
ENV API_GATEWAY_URL=http://api-gateway
